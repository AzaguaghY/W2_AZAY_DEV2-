<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .logo-container {
            background-color:grey; /* Couleur de fond */
            padding: 20px 0; /* Espacement vertical autour du logo */
            text-align: center; /* Centrage du logo */
        }
    </style>
    
</head>
<body>
    <div class="logo-container">
        <img src="https://clg.qc.ca/wp-content/themes/clg.qc.ca/dist/images/logo.svg" alt="Collège Lionel Groulx" style="width: 150px; height: auto;">
    </div>
    <div class="container my-5">
        <h1 class="text-center mb-4"><i class="fas fa-book"></i> Liste des Livres</h1>

        <table class="table table-striped table-hover">
            <thead class="thead-dark">
                <tr>
                    <th scope="col"><i class="fas fa-book"></i> Titre</th>
                    <th scope="col"><i class="fas fa-user"></i> Auteur</th>
                    <th scope="col"><i class="fas fa-calendar-alt"></i> Année de publication</th>
                    <th scope="col"><i class="fas fa-tags"></i> Genre</th>
                </tr>
            </thead>
            <tbody>
                <% if (livres && livres.length > 0) { %>
                    <% livres.forEach(function(livre) { %>
                        <tr>
                            <td><%= livre.titre %></td>
                            <td><%= livre.auteur %></td>
                            <td><%= livre.annee %></td>
                            <td><%= livre.genre %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="4" class="text-center">Aucun livre disponible.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>

        <div class="text-center mt-4">
            <a href="/" class="btn btn-primary">
                <i class="fas fa-arrow-left"></i> Retour à la page d'accueil
            </a>
        </div>
    </div>
</body>
</html>








